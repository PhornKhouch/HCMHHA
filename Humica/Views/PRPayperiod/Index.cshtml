<script type="text/javascript">
</script>

@model Humica.Logic.PR.PRPayPeriodObject

@*@Html.Partial("GridItems", Model.ListPayPeriod)*@

@Html.DevExpress().PageControl(
    settings =>
    {
        settings.Name = "pcScrollingMaterial1";
        settings.Width = Unit.Percentage(100);

        settings.EnableTabScrolling = true;
        settings.TabAlign = TabAlign.Justify;

        settings.Styles.Tab.CssClass = "scrolling_Tab";

        settings.TabPages.Add(Humica.EF.Models.SY.SYSettings.getLabel("Pay Period")).SetContent(() =>
        {
            @Html.RenderPartial("GridItems", Model.ListPayPeriod);
        });
    }).GetHtml()