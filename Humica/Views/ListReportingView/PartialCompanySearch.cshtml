@{
    var gridExtension = Html.DevExpress().GridView(
           settings =>
           {
               settings.Name = "CompanyGridListSelector";
               settings.KeyFieldName = "Company";

               settings.Width = Unit.Percentage(100);

               settings.CallbackRouteValues = new { Action = "PartialCompanySearch", Controller = "LoadingList", ScreenId = ViewData[Humica.EF.Models.SY.SYConstant.SCREEN_ID].ToString() };
               settings.SettingsSearchPanel.Visible = true;
               settings.SettingsBehavior.ConfirmDelete = true;

               settings.CommandColumn.Visible = true;
               settings.SettingsDataSecurity.AllowReadUnlistedFieldsFromClientApi = DefaultBoolean.True;
               settings.Settings.ShowGroupPanel = false;
               settings.Settings.ShowFooter = true;
               settings.SettingsContextMenu.Enabled = false;
               settings.SettingsContextMenu.RowMenuItemVisibility.DeleteRow = false;
               settings.SettingsContextMenu.RowMenuItemVisibility.EditRow = false;
               settings.SettingsContextMenu.RowMenuItemVisibility.NewRow = false;
               settings.Settings.ShowHeaderFilterButton = true;
               settings.SettingsPopup.HeaderFilter.Height = 200;
               settings.CommandColumn.SelectAllCheckboxMode = GridViewSelectAllCheckBoxMode.AllPages;
               settings.CommandColumn.ShowSelectCheckbox = true;


               settings.CommandColumn.Width = 50;

               settings.Columns.Add(col =>
               {
                   col.FieldName = "Company";
                   col.Width = 100;
                   col.Caption = Humica.EF.Models.SY.SYSettings.getLabel("Code");
               });

               settings.Columns.Add(col =>
               {
                   col.Width = 200;
                   col.FieldName = "Description";
                   col.Caption = Humica.EF.Models.SY.SYSettings.getLabel("Description");
               });

               settings.Columns.Add(col =>
               {
                   col.Width = 200;
                   col.FieldName = "SecDescription";
                   col.Caption = Humica.EF.Models.SY.SYSettings.getLabel("Second Description");
               });

               settings.SettingsBehavior.EnableCustomizationWindow = false;

               settings.PreRender = (s, e) =>
               {

               };


               settings.ClientSideEvents.SelectionChanged = "OnSelectionChangedCompany";

               settings.SettingsPager.Position = PagerPosition.Bottom;
               settings.SettingsPager.FirstPageButton.Visible = true;
               settings.SettingsPager.LastPageButton.Visible = true;


               settings.SettingsBehavior.AllowFocusedRow = true;

               settings.SettingsBehavior.ColumnResizeMode = ColumnResizeMode.Control;


               settings.SettingsBehavior.AllowEllipsisInText = true;

               settings.SettingsPager.PageSizeItemSettings.Visible = true;
               settings.SettingsPager.PageSize = 5;


           });
}

@gridExtension.Bind(ViewData["COMPANY_SELECT"]).GetHtml()
@*@gridExtension.BindToLINQ(string.Empty, string.Empty, (s, e) =>
{
  //  e.QueryableSource = CLOUDVIEW.Models.MD.ClsExItemMaster.MaterialsStock;
    e.KeyExpression = "MaterialCode";
}).GetHtml()*@
